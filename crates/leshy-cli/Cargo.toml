[package]
name = "leshy-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "leshy"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
leshy-core = { path = "../leshy-core" }
